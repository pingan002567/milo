# 秘书 Agent 设计（对话式系统操作面）

> 定位（2026-07-19 用户决策）：**秘书是一个专门服务用户"管理和使用本系统全部能力"的
> AI Agent，同样基于 DeerFlow**；与用户的交互是对话形式。
> 领域名仍为 Secretariat（展示名"秘书"）；本文规划其从"系统角色"升格为
> "对话式 Agent"的完整设计。

---

## 一、定位与不变量

**秘书是什么**：你在系统里唯一的自由对话对象（编制设计 §3.5 的"组长↔秘书唯一自由
文本通道"落到实处）。它懂整个系统：团队、名册、任务群、Agent 市场/库、评测、设置，
你用自然语言说事，它用工具替你操作或替你查询。

**秘书不是什么（三条不变量，全部继承既有决策）**：

1. **不是团队成员**——不占编制、不接任务信封、不产出交付物；它是"办公室"本身。
2. **不拥有决定权**——成员管理红线不动摇：秘书对人事只能**提案**，执行永远经你
   确认；对成员请示（escalation）**不能代答**，只能转述与建议。
3. **不绕过制度层**——路由、验收、状态机、隔离仍是 Office 的确定性代码。
   秘书经受控工具调用系统 API，**无任何越过 API 直达成员/文件的通道**。

一句话架构观：**秘书 = 理解与表达层（LLM），Office = 制度与执行层（代码）**。
之前"分解是秘书唯一用 LLM 的环节"升级为"秘书是一个完整 agent，但它对系统的
一切作用都收敛为白名单工具调用"。

## 二、信任边界的延伸

既有规则：成员自由文本 = 不可信数据，只有结构化事件驱动行为。秘书成为 LLM agent
后新增两条：

- **工具调用即意图**：秘书说了什么不驱动系统，只有它的工具调用驱动系统；
  工具入参经 schema 校验（同成员信封的强校验思路）。
- **工具结果即数据**：秘书读到的一切（成员汇报、市场描述、产物内容）都可能含
  prompt 注入——注入最坏也只能骗秘书发起一次工具调用，而写操作有分级护栏
  （见下），确认级动作永远要过你的确认卡，不存在"被注入→自动人事变动"的路径。

## 三、工具面（Milo Tools，白名单三级）

秘书的能力 = 工具清单，渲染进其 DeerFlow config（复用成员的权限收敛机制）。
工具实现为对 milod API 的本地回环调用（`http://127.0.0.1:<port>`），权限面
恒等于 API 面，不开后门。

| 级别 | 规则 | 工具 |
|---|---|---|
| L0 只读 | 直接执行 | list_members / list_groups / get_group(含事件) / get_todos / list_market / list_library / get_artifact / get_eval_report / test_connection |
| L1 低危写 | 直接执行 + 事件留痕 | **create_task_group(request)**（仍走计划批准回合，不是绕过）、download_template、star/unstar |
| L2 确认级 | 只产生**提案卡**，你点确认后由系统执行 | propose_hire(agent, name) / propose_suspend / propose_resume / propose_dismiss / propose_remove_template |

**明确不给的工具**：reply_escalation（成员请示只能你答）、改模型绑定/密钥、
删除任务群/事件（审计不可删）、任何 bash/文件系统工具。

提案卡复用现有决策卡形态（events 表新增 `proposal` 事件类型 + 确认/驳回两个
API）；确认后系统调用对应人事 API，事件流留"你批准了秘书的提案"痕迹。

## 四、运行形态

- **一个团队一个秘书实例**：`orgs/<org>/secretary/` 私有工作区，复用
  SubprocessAdapter/worker 全套机制（子进程隔离、checkpointer、事件归一化），
  但 enroll 时注入的是 Milo Tools 而非成员工具；persona 是内置的（系统自带
  SecretaryPack，不来自市场，不可辞退）。
- **常驻会话**：秘书对话是一个固定 thread（`secretary-main`），checkpointer
  即记忆——它记得你们聊过什么、你偏好什么。上下文膨胀由 harness 侧机制
  （摘要/截断）兜底，v1 再做话题分段。
- **对话存储零新基建**：秘书对话作为一个特殊任务群（`group_id: "secretary"`，
  事件类型 chat/status/system/proposal），复用 events 表、WS 推送、断线补发、
  Md 渲染——「💬 秘书」页就是一个特化的任务群视图。
- **分解职责的归宿**：现在 hub 直接调 chat/completions 分解。迁移后：你在对话里
  说"帮我写个脚本再评审"，秘书判断这是派活 → 调 create_task_group → 系统走
  既有分解与计划批准回合。hub._ask_llm 退役，分解 prompt 变成秘书 persona 的
  一部分（v2，见分期）。

## 五、交互设计（「💬 秘书」页）

- 任务输入框升级为**完整对话流**：消息气泡（Md 渲染）+ 流式回复 + 常驻输入框。
- 秘书的工具调用在流里显示为**动作行**（"🔍 查询了团队状态"、"📋 已创建任务群
  g-xxx"，点击可跳转），不是正文——控制/数据分离在 UI 上的表达。
- L2 提案渲染为**提案卡**（同决策卡形态：标题=秘书建议招募"小周"，正文=理由，
  按钮=批准/驳回）。
- 主动性（v1）：mention 级事件（成员请示、交付验收）注入秘书对话，由秘书
  转述+给建议——通知不再是干巴巴的系统消息，而是"秘书跟你说话"。

## 六、分期

| 期 | 内容 | 验收 |
|---|---|---|
| S1 最小闭环 | 秘书实例（内置 SecretaryPack + Milo Tools L0/L1）+ 对话 UI + 常驻记忆；"帮我列下团队谁在忙""把 CSV 脚本的活派下去"全程对话完成 | 对话中完成 查询→派活→进群看计划 |
| S2 提案与主动性 | L2 提案工具 + 提案卡确认流；mention 事件注入对话由秘书转述 | "帮我招个前端"→提案卡→批准→名册出现新成员；成员请示时秘书在对话里@你 |
| S3 分解内收 | create_task_group 升级为秘书自带分解（propose_plan 工具产计划卡），hub._ask_llm 退役；市场检索/评测运行工具 | 计划质量不低于现状（用既有任务回归对比） |

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| 秘书被产物/市场内容注入 | 工具结果 data fencing；L2 必过确认卡；无文件/bash 工具 |
| 秘书幻觉编造系统状态 | 动作行展示真实工具调用与结果摘要；关键数字（成员数/任务态）UI 侧栏另有权威展示，可对照 |
| 对话与任务群双入口混乱 | 秘书对话只作"入口与转述"，任务执行细节仍在任务群页；动作行提供跳转 |
| 秘书实例崩溃 | 复用成员崩溃恢复机制；对话历史在 events 表，重启即续 |
| token 成本（常驻对话） | 秘书用与成员相同的绑定档位；v1 做话题分段与摘要 |
