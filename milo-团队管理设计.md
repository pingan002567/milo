# 团队管理设计（新建 · 切换 · 设置 · 删除）

> 2026-07-20。现状：一个 milod 可服务多团队，但 UI 只有一个原生下拉框能**切换**
> 已有团队；新建只能命令行 `milo init`，改名/删除只能手改文件。本文补齐这一层。

## 一、概念与边界

**团队（org）= 一个完全独立的工作世界**：独立的名册、成员实例与记忆、秘书实例与
对话、任务群与事件审计、模型绑定与密钥。落盘于 `~/.milo/orgs/<slug>/`。

跨团队共享的只有两样：**Agent 库**（模板是用户全局资产）与**本地默认权限**。

## 二、命名：显示名 vs 标识（复用成员那套）

团队名当前既是显示名又是目录名与 API 路径段，导致必须 ASCII。改为与成员一致的
双名制：

- **显示名**：可中文（"研发小队"），存 `org.yaml` 的 `metadata.displayName`，UI 全用它
- **标识（slug）**：ASCII，目录名与 API 路径段，新建时按显示名自动生成、可手改、创建后不可变

## 三、功能规划

### 3.1 新建团队（P0）

入口：切换器下拉底部「+ 新建团队」（就在切换的地方，符合直觉）；设置页工作区卡
提供第二入口。

表单三项，尽量少：

| 字段 | 说明 |
|---|---|
| 显示名 | 必填，可中文；同时自动推导 slug（可展开修改） |
| 模型绑定 | 默认**继承当前团队**（一键沿用，密钥同 secret_env 直接复用）；可改 |
| 初始成员 | 可选，从 Agent 库勾选若干模板直接招募（跳过"建完再去招人"） |

创建 = 落 `org.yaml` + `bindings.yaml` + 建目录，随即切换过去。

### 3.2 切换器升级（P0）

原生 select → 下拉面板，每项：状态点 · 显示名 · 成员数 · **待办角标**。

**跨团队待办可见性**是这里的真正价值：现在你在 A 团队干活时，B 团队成员的请示
完全看不见。方案：`GET /api/orgs` 直接查各团队 SQLite 统计 `input_required` 数量
（轻量，不必拉起成员子进程），前端定时刷新。

### 3.3 团队设置（P1）

设置弹窗新增「团队」tab（当前工作区卡升级）：

- 显示名编辑（slug 只读展示）
- 编制上限 `maxParallelMembers`（现在只能改文件）
- 数据目录：只读路径 + 「在 Finder 打开」
- 用量概览：成员数、任务群数、事件数、产物占用空间
- 危险区 → 删除团队

模型绑定仍在「AI 配置」tab，但需明示它是**按团队**的（当前文案已提，加强）。

### 3.4 删除团队（P1）

危险动作，参照 GitHub 删仓库：

- 二次确认需**输入团队标识**才可提交
- 明确列出将删除的内容：名册、成员工作区与记忆、秘书对话、事件审计、交付产物
- 提供「保留交付产物」勾选（把 `artifacts/` 移到 `~/.milo/archive/<slug>-<日期>/`）
- 先停掉运行中的成员与秘书，再删目录
- 删除的是当前团队则自动切到其他团队；删掉最后一个则回到空态向导

### 3.5 空态与首次运行（P2）

全新安装（无任何团队）时不该是空白界面，而是**新建第一个团队的向导**：
显示名 → 模型与密钥 → 可选招募首批成员。目前靠 `dev.sh` 自动建 demo 兜底，
正式产品需要这个向导。

### 3.6 全局模型默认（P2）

与权限默认同构：`settings/defaults.yaml` 增加模型段，新建团队零配置继承；
团队可覆盖。消除"每建一个团队都要配一遍模型"的重复。

### 3.7 团队模板（P3）

产品方案 §2.4 的"组织模板（成建制分发）"：一次建好带既定成员编制的团队
（研究小组 = 文献检索 + 分析 + 写作）。是 Agent 市场的延伸形态——市场卖单个
Agent，模板卖**一整支队伍**。等 Registry v0 落地后再做。

## 四、API 形状

| 端点 | 作用 |
|---|---|
| `POST /api/orgs` `{displayName, slug?, bindings?, members?}` | 新建团队 |
| `PATCH /api/orgs/{org}` `{displayName?, limits?}` | 团队设置 |
| `DELETE /api/orgs/{org}?keep_artifacts=true` | 删除团队 |
| `GET /api/orgs`（扩展） | 增加 `displayName` / `pending` / 用量统计 |

## 五、优先级

| 期 | 内容 | 价值 |
|---|---|---|
| P0 | 新建团队 + 切换器升级（跨团队待办角标） | 补齐闭环；跨团队请示不再失联 |
| P1 | 团队设置 tab + 删除团队 | 全生命周期可视化管理 |
| P2 | 首次运行向导 + 全局模型默认 | 新用户 onboarding 与去重复配置 |
| P3 | 团队模板（成建制分发） | 与 Registry/市场联动的产品放大器 |
