# 右栏检查器设计（Inspector）

> 2026-07-20 决策。诊断：改造前右栏只有两种形态——任务群屏显示任务信息，
> 其余六屏共用"成员列表 + 静态说明"，在团队页与主区完全重复、在市场/秘书页
> 完全无关。它是占位符，不是设计。

## 一、定位与纪律

右栏 = **当前对象的检查器**：回答"我正在看的这个东西，它的档案、相关物、能做什么"。

三条纪律：
1. **不做导航**——那是左栏的职责；右栏只服务当前主体。
2. **不重复主区**——主区已呈现的内容不在右栏再列一遍。
3. **每屏强相关**——没有相关内容宁可给一句话说明，不塞通用列表。

统一的三段式组织：**档案（是什么）→ 相关物（关联对象）→ 快捷动作（能做什么）**。

## 二、分屏设计

### 聊天页面（主体明确 = 对话方，名片式）

| 屏 | 档案 | 相关物 | 动作 |
|---|---|---|---|
| 💬 秘书 | 秘书定位一句话 + 工具清单（L0 只读 / L1 低危写分组） | 本次会话的工具调用记录（可跳转任务群） | — |
| 私聊成员 | **成员名片**：状态徽章、出厂模板、能力 chips、权限三项摘要 | 名下最近任务（状态+跳转）、交付过的产物 | 编辑档案、停职/复岗 |

私聊的成员名片价值最高：调教它时档案就在旁边（llm-space"编辑即实验"在组织语境下的形态）。

### 功能页面（主体是列表 → 概览优先）

| 屏 | 右栏内容 |
|---|---|
| 👥 团队 | **团队健康度**：在岗/停职/待加入计数、编制余量、忙碌成员、**权限告警**（谁开了 host bash）；不再重复列成员 |
| 🛒 市场 | **库存概览**：已下载/收藏/未实测计数 + 说明 |
| 🗂 名册 | org.yaml 原文预览（只读可复制）——名册页本就是"看文件"，右栏给原始事实 |
| ✅ 待办 | 按成员分组计数 + 最久未处理提醒 |
| 📋 任务群 | 重排：**进度时间线** → **产物清单**（可预览/打开）→ 参与成员 → 元数据（ID 等）降至最底 |

## 三、交互

- **可折叠**：折叠钮 + `Cmd+\`，状态存 localStorage；窄屏（<1080px）仍自动隐藏。
- **空态**：无可检查对象时给该屏一句话说明，不留白板。
- **不做**：右栏不放主操作按钮（主操作属于主区）、不做多 tab（需要 tab 说明该拆页面）。

## 四、实施顺序

1. 私聊成员名片
2. 任务群右栏重排 + 产物清单
3. 团队健康度（含权限告警）
4. 秘书工具记录 / 市场库存 / 名册原文 / 待办分组
5. 折叠与快捷键
