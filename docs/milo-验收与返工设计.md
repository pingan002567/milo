# 验收与返工设计（用户确认 · 返工 · 自动归档）

> 2026-07-20 用户决策：执行完成的任务需要用户确认；确认后手动归档，或 24 小时
> 后自动归档；产出不符预期时用户补充描述/资料，秘书在**同一个群**重新组织执行。

## 一、哲学变更：秘书验收降级为初筛

改造前：秘书按 `output_spec` 机械校验（产物是否存在、格式是否符合）→ accepted →
全终态即自动归档。**机器的判定就是终局**。

改造后：

```
成员交付 → 秘书初筛（产物/格式）
    ├─ 不过 → 直接退回成员重做（不打扰你）
    └─ 通过 → 群转 review「待你确认」并 @你
                ├─ ① 确认满意        → 归档
                ├─ ② 补充要求/资料   → 同群返工（第 N 轮）→ 回到执行中
                └─ ③ 不处理          → 24 小时后自动归档（默认满意）
```

初筛的价值不变（把明显没干完的挡回去，不打扰你）；但**"这活儿办得对不对"只有你说了算**。

## 二、状态机与数据

- `groups.status` 新增 **`review`**（待你确认）；`groups.metadata.review_since`
  记录进入时间，作为自动归档倒计时基准。
- **返工不新建群、不新建任务**：同一 task 新开一个 run（`attempts + 1`），
  你的补充作为 envelope 修订事件留痕；**上一轮产物经 inputs.artifacts 回传给成员**
  ——成员是"改稿"而不是"从头重做"（复用已有的 artifact 引用授权机制）。
- 自动归档双保险：Hub 后台每 10 分钟扫一次 + groups 列表接口惰性检查
  （防 milod 重启期间错过）。

## 三、API

| 端点 | 作用 |
|---|---|
| `POST /groups/{gid}/confirm` | 确认满意 → 归档 |
| `POST /groups/{gid}/rework` `{feedback, artifacts?}` | 补充要求/资料 → 同群返工 |
| （内部）自动归档扫描 | review 态超时 → 归档，留 system 事件说明 |

## 四、交互

- **验收卡**（群会话底部，review 态出现）：本轮产物清单（可预览）+
  `[满意，归档]` `[不满意，补充要求…]` + 「N 小时后自动归档」倒计时。
- **轮次分节**：第 1 轮交付 → 你的反馈 → 第 2 轮交付，改稿历史一目了然。
- 左栏 review 群用独立状态点；待办聚合把"待确认"一并纳入。
- 通知等级：mention（@你），不是高优先 notify——它不阻塞，只是等你看。

## 五、优化建议（超出原始需求的部分）

1. **返工 = 改稿而非重做**（已并入上文）：上一轮产物回传给成员，避免它从零再来
   一遍、也避免丢掉你认可的部分。
2. **「跳过确认」开关**：设置里可开——秘书初筛通过即归档（回到改造前行为）。
   小任务频繁确认会烦人，把选择权给你。
3. **自动归档时长可配置**：24 小时是默认值，设置里可调（含"永不自动归档"）。
   UI 明示倒计时，避免"周末回来全归档了"的意外感。
4. **补充资料预留文件通道**：v0 先做文字反馈；接口 `artifacts` 字段预留，
   v1 支持拖文件进来作为返工输入（复用组织级 artifacts 目录）。
5. **归档群可重新激活**：在归档群里继续补充要求即重开为执行中——事后发现问题
   不必新开群、丢掉上下文。
6. **返工上限提示**：同一任务返工超过 3 轮时，秘书提示"可能是目标描述有歧义，
   建议重新分解"并给出「重新分解」入口（复用刚做的 retry 通路）。

## 六、分期

| 期 | 内容 |
|---|---|
| R1 | review 状态 + 验收卡（确认/返工）+ 同群返工带产物回传 + 24h 自动归档 |
| R2 | 轮次分节 UI + 跳过确认开关 + 时长可配置 + 归档群重新激活 |
| R3 | 文件资料补充 + 返工上限提示与重新分解建议 |
